rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For hackathon, allow all read/write
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
